version: v1.0
workflows:
  hello_world:
    type: cwl
    config:
      file: cwl/example.cwl
    bindings:
      - step: /print_message
        target:
          deployments: container

deployments:
  container:
    type: docker
    config:
      image: ubuntu:latest
