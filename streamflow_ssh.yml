version: v1.0

workflows:
  workflow1:
    type: cwl
    config:
      file: cwl/task_train.cwl
      settings: cwl/settings.yml
    bindings:
      - step: /
        target:
          deployment: ssh
          service: sklearn

deployments:
  ssh:
    type: ssh
    config:
      nodes:
        - genoa-login.h-its.org
      username: doserbd
      checkHostKey: false
      services:
        sklearn: environments/sklearn/launch.sh
