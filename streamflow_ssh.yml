version: v1.0

workflows:
  workflow1:
    type: cwl
    config:
      file: cwl/task_say_hello.cwl
      settings: cwl/settings.yml
    bindings:
      - step: /
        target:
          deployment: ssh

deployments:
  ssh:
    type: ssh
    config:
      hostname: rh04715.h-its.org
      username: doserbd
