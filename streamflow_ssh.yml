version: v1.0

workflows:
  workflow1:
    type: cwl
    config:
      file: cwl/task_hostname.cwl
      settings: cwl/settings.yml
    bindings:
      - step: /
        target:
          deployment: ssh

deployments:
  ssh:
    type: ssh
    config:
      nodes:
        - genoa-login.h-its.org
      username: doserbd
      checkHostKey: false
